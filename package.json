{
  "name": "file-explorer",
  "private": true,
  "version": "0.1.0",
  "description": "node + vue3 file-explorer",
  "scripts": {
    "start": "npm run build && node start",
    "dev": "node dev",
    "build": "node build"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "fs-extra": "^9.1.0",
    "socket.io-client": "^3.1.1",
    "vite": "^2.2.1"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.7",
    "nodemon": "^2.0.7",
    "socket.io": "^3.1.1"
  }
}